import Template from "../components/Template";

export default function OutroPerfil({ children }) {
    const baseUrl = "http://localhost:8080/api";

    return (
        <Template>
            asdas
        </Template>
    );
}