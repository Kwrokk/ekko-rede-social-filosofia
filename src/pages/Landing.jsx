import DefaultContainer from "../components/DefaultContainer";

export default function Landing() {
    return (
        <DefaultContainer>
            <h1>This is the landing page.</h1>
            <p>We'll add stuff here soon.</p>
        </DefaultContainer>
    );
}